<!--
  Posted after the market resolves. Same channel as the pre-run.
  Replace every <TBD> with a real value pulled from the resolved
  runs/002/report.json before posting.

  TODO(maintainer): this is a skeleton drafted in the absence of
  the canonical templates from the brief. Review and replace if
  needed, then delete this comment.
-->

**Run 002 — resolved**

`<TBD ticker>` resolved `<TBD yes|no>` at `<TBD ts UTC>`.

**Brier**
- Meta-ensemble: `<TBD>`
- Climatology baseline: `<TBD>`
- Δ: `<TBD>` *(positive = ensemble better)*

**P&L** — `$<TBD>` *(paper)*.

**Takeaway** — `<TBD: 1–2 lines. Was the model's confidence
warranted? Did climatology win? What does this update for the
ensemble's per-segment weights?>`

Phase 1 sample: `<TBD N>` resolved positions / `>50 required`.

Full report: `predictor/runs/002/REPORT.md`.
