<!--
  Channel target: #🌱-product-updates (or the project's Kalshi-POC
  channel once it exists). Voice: builder log. Keep it terse.
  Replace every <TBD> with a real value pulled from runs/002/report.json
  before posting. If a value is genuinely unknown, say "n/a" not <TBD>.

  TODO(maintainer): this is a skeleton drafted in the absence of the
  canonical templates from the brief. Review wording and structure
  against the templates you have on file, replace if needed, then
  delete this comment.
-->

**Run 002 — opening**

Pulling a paper position on Kalshi `<TBD ticker>` — *<TBD market question>*.

Resolution: NWS, `<TBD time UTC>`.

**Model**
- Meta-ensemble (ECMWF + GraphCast + GFS + JMA) → `p_yes = <TBD>`
- Climatology baseline → `p_yes = <TBD>`
- Market mid at entry → `<TBD>` (bid `<TBD>` / ask `<TBD>`)

**Edge**
- vs market: `<TBD bps>`
- vs climatology: `<TBD bps>`

**Position** — `<TBD yes|no>`, `$<TBD>` notional, paper.

**Why this market** — `<TBD: 1–2 lines of edge thesis. State the
asymmetry the model sees, not bullishness on weather.>`

Trace: `predictor/runs/002/REPORT.md` · tag `run-002`.
