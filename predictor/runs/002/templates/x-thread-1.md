<!--
  X / Twitter — tweet 1 of a 2-tweet thread.
  Posted at run open. ≤ 280 chars (the post-x.mjs script does NOT
  truncate manual posts — keep it under 280 yourself).
  English. No emojis or 1 max. Builder voice.

  TODO(maintainer): skeleton drafted by Claude. Review wording,
  replace <TBD> with values from runs/002/report.json, count chars,
  delete this comment before posting.
-->

Augure run-002 — opened a paper position on Kalshi `<TBD ticker>`.

Meta-ensemble (ECMWF + GraphCast + GFS + JMA): p_yes = `<TBD>`.
Climatology: `<TBD>`.

Edge vs market: `<TBD bps>`.

Resolution `<TBD UTC>`. Builder log on resolution.

github.com/Elladriel80/augure
